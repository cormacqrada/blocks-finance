# Backend FastAPI Template

This template is used when you run:

```bash
blocks-app new backend fastapi my-app
```

It scaffolds a FastAPI backend that is ready to integrate with the Blocks runtime and schemas defined in `schema/`.
